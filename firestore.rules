rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY: Allow all operations on all documents
    // WARNING: This is NOT secure for production!
    // TODO: Restore proper security rules before launching
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
